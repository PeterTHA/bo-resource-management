# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# MongoDB Connection (เก็บไว้เผื่อต้องการใช้ในอนาคต)
MONGODB_URI=mongodb://localhost:27017/resource-management

# Database - ใช้ SQLite สำหรับการพัฒนาเพื่อความง่าย
POSTGRES_PRISMA_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-change-this-in-production

# Vercel Postgres (Neon) - เปลี่ยนค่าเหล่านี้ตามการตั้งค่าของคุณ
POSTGRES_URL=postgres://username:password@hostname/database?sslmode=require
POSTGRES_URL_NON_POOLING=postgres://username:password@hostname/database?sslmode=require
POSTGRES_USER=username
POSTGRES_HOST=hostname
POSTGRES_PASSWORD=password
POSTGRES_DATABASE=database

# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN=your-blob-read-write-token

# Vercel KV (Redis)
REDIS_URL=your-redis-url

# Vercel KV (Redis) - ถ้าใช้ KV API
# KV_URL=
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN= 